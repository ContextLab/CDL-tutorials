ax.add_patch(
    patches.Rectangle(
        (0.4, 0.15),   # (x,y)
        0.2,          # width
        0.2,          # height
        facecolor="#197F50",
    )
)

ax.add_patch(
    patches.Rectangle(
        (0.4, 0.4),   # (x,y)
        0.2,          # width
        0.2,          # height
        facecolor="#66A98A",
    )
)
